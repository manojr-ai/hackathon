# ============================================================================
# STREAMLIT SECRETS CONFIGURATION
# ============================================================================
# File: .streamlit/secrets.toml
# Purpose: Lakebase PostgreSQL connection credentials
# ============================================================================

# Lakebase PostgreSQL Connection
lakebase_user = "YOUR_LAKEBASE_USERNAME"
lakebase_password = "YOUR_LAKEBASE_PASSWORD"

# Optional: Override connection details if needed
lakebase_host = "lakebase-06f92301-4b5f-47eb-95cb-bc02c23df5b8.cloud.databricks.com"
lakebase_port = 5432
lakebase_database = "healthgpt"

# Usage in app:
# conn = psycopg2.connect(
#     host=st.secrets["lakebase_host"],
#     port=st.secrets["lakebase_port"],
#     database=st.secrets["lakebase_database"],
#     user=st.secrets["lakebase_user"],
#     password=st.secrets["lakebase_password"]
# )
